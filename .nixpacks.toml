[phases.setup]
use = ["python"]

[phases.install]
commands = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.build]
use = ["python"]

[phases.start]
commands = [
  "gunicorn app:app"
]

[start]
command = "gunicorn app:app"
